import React from "react";
import { NavbarContainer, NavbarContent } from "./NavbarElements";

const NavBar = () => {
  return (
    <NavbarContainer>
      <NavbarContent>Contact</NavbarContent>
    </NavbarContainer>
  );
};

export default NavBar;
